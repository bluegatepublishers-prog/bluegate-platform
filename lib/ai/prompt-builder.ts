import type{KnowledgePackage,ProviderRequest}from"./types";
export function buildQuestionPaperPrompt(configuration:Record<string,unknown>,knowledge:KnowledgePackage):ProviderRequest{const systemPrompt=["You are Bluegate's educational assessment engine.","Use only the supplied approved knowledge package.","Never invent textbook facts, chapters, answers, or learning outcomes.","Return valid JSON matching the requested paper structure.","Honor total marks, blueprint, chapter weightage, difficulty/Bloom distribution, and options.","If grounded evidence is insufficient, return a structured insufficiency warning instead of fabricating content."].join("\n");const userPrompt=["TASK: Build a complete question paper.","CONFIGURATION_JSON:",JSON.stringify(configuration,null,2),"APPROVED_KNOWLEDGE_JSON:",JSON.stringify(knowledge,null,2),"REQUIRED_RESPONSE_JSON:",JSON.stringify({title:"string",instructions:["string"],sections:[{name:"string",questionType:"string",questions:[{text:"string",marks:1,answer:"string|null",explanation:"string|null",sourceChapterId:"string"}]}],totalMarks:0,validationNotes:["string"]},null,2)].join("\n\n");return{systemPrompt,userPrompt,responseFormat:"json"}}
