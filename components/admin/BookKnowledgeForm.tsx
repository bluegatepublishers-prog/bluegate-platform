import { saveChapter, saveQuestion } from "@/app/admin/books/[id]/knowledge-actions";
const field="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3";const types=["MCQ","FILL_BLANK","TRUE_FALSE","MATCH","MULTIPLE_SELECT","VERY_SHORT","SHORT","SHORT_ANSWER","LONG","LONG_ANSWER","ASSERTION_REASON","CASE_STUDY","CASE_BASED","COMPETENCY","HOTS","PRACTICAL_ACTIVITY"];
export function ChapterForm({bookId,chapter}:{bookId:string;chapter?:{id:string;chapterNumber:number;title:string;slug:string;startPage:number|null;endPage:number|null;extractedText:string|null;reviewedText:string|null;summary:string|null;keywords:string[];approved:boolean;sortOrder:number}}){const action=saveChapter.bind(null,bookId,chapter?.id);return <form action={action} className="space-y-6"><div className="grid gap-5 rounded-3xl border bg-white p-7 md:grid-cols-2"><F label="Chapter number"><input required min="1" type="number" name="chapterNumber" defaultValue={chapter?.chapterNumber} className={field}/></F><F label="Title"><input required name="title" defaultValue={chapter?.title} className={field}/></F><F label="Slug"><input name="slug" defaultValue={chapter?.slug} placeholder="Generated from title" className={field}/></F><F label="Sort order"><input min="0" type="number" name="sortOrder" defaultValue={chapter?.sortOrder} className={field}/></F><F label="Start PDF page"><input min="1" type="number" name="startPage" defaultValue={chapter?.startPage??""} className={field}/></F><F label="End PDF page"><input min="1" type="number" name="endPage" defaultValue={chapter?.endPage??""} className={field}/></F><F label="Keywords (comma separated)"><input name="keywords" defaultValue={chapter?.keywords.join(", ")} className={field}/></F><label className="mt-7 flex items-center gap-3"><input type="checkbox" name="approved" defaultChecked={chapter?.approved}/><span className="font-semibold">Approved for AI grounding</span></label><Area label="Extracted text" name="extractedText" value={chapter?.extractedText}/><Area label="Reviewed / approved text" name="reviewedText" value={chapter?.reviewedText}/><Area label="Summary" name="summary" value={chapter?.summary}/></div><div className="rounded-2xl border border-dashed bg-slate-50 p-5"><button type="button" disabled className="rounded-xl bg-slate-200 px-4 py-2 font-semibold text-slate-500">Extract from PDF — coming later</button><p className="mt-2 text-sm text-slate-500">Use the page range and paste selectable PDF text manually. OCR is not attempted.</p></div><button className="rounded-xl bg-blue-600 px-6 py-3 font-semibold text-white">Save chapter</button></form>}
export function QuestionForm({bookId,chapters,question}:{bookId:string;chapters:{id:string;chapterNumber:number;title:string}[];question?:{id:string;chapterId:string;questionType:string;questionText:string;options:unknown;correctAnswer:string|null;explanation:string|null;marks:number;difficulty:string;bloomLevel:string|null;competency:string|null;approved:boolean}}){const action=saveQuestion.bind(null,bookId,question?.id);const options=Array.isArray(question?.options)?question.options.filter((x):x is string=>typeof x==="string").join("\n"):question?.options&&typeof question.options==="object"&&!Array.isArray(question.options)&&Array.isArray((question.options as {left?:unknown}).left)&&Array.isArray((question.options as {right?:unknown}).right)?(question.options as {left:string[];right:string[]}).left.map((left,index)=>`${left} => ${(question.options as {right:string[]}).right[index]??""}`).join("\n"):"";let correctAnswer=question?.correctAnswer??"";if(question?.questionType==="MULTIPLE_SELECT"&&correctAnswer){try{const parsed=JSON.parse(correctAnswer);if(Array.isArray(parsed))correctAnswer=parsed.filter((item):item is string=>typeof item==="string").join(", ");}catch{}}return <form action={action} className="space-y-6 rounded-3xl border bg-white p-7"><div className="grid gap-5 md:grid-cols-2"><F label="Chapter"><select required name="chapterId" defaultValue={question?.chapterId??""} className={field}><option value="">Select chapter</option>{chapters.map(c=><option key={c.id} value={c.id}>{c.chapterNumber}. {c.title}</option>)}</select></F><F label="Question type"><select name="questionType" defaultValue={question?.questionType??"MCQ"} className={field}>{types.map(x=><option key={x}>{x}</option>)}</select></F><F label="Marks"><input name="marks" type="number" min="1" defaultValue={question?.marks??1} className={field}/></F><F label="Difficulty"><select name="difficulty" defaultValue={question?.difficulty??"MEDIUM"} className={field}>{["EASY","MEDIUM","DIFFICULT"].map(x=><option key={x}>{x}</option>)}</select></F><F label="Bloom level"><input name="bloomLevel" defaultValue={question?.bloomLevel??""} className={field}/></F><F label="Competency"><input name="competency" defaultValue={question?.competency??""} className={field}/></F></div><Area label="Options (one per line; Match uses Left => Right)" name="options" value={options}/><Area label="Correct answer (multiple-select: comma separated)" name="correctAnswer" value={correctAnswer}/><Area label="Question" name="questionText" value={question?.questionText} required/><Area label="Explanation" name="explanation" value={question?.explanation}/><label className="flex items-center gap-3"><input type="checkbox" name="approved" defaultChecked={question?.approved}/><span className="font-semibold">Approved</span></label><button className="rounded-xl bg-blue-600 px-6 py-3 font-semibold text-white">Save question</button></form>}
function F({label,children}:{label:string;children:React.ReactNode}){return <label className="text-sm font-semibold text-slate-700">{label}{children}</label>}function Area({label,name,value,required}:{label:string;name:string;value?:string|null;required?:boolean}){return <label className="block text-sm font-semibold text-slate-700">{label}<textarea required={required} name={name} defaultValue={value??""} rows={5} className={field}/></label>}
