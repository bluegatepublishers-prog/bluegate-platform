"use client";
import Link from"next/link";import ContentStudioTree from"@/components/admin/books/ContentStudioTree";import type{ContentTreeNode}from"@/lib/content-studio-tree";
export default function ContentStudioShell({bookId,root,selectedKey,children}:{bookId:string;root:ContentTreeNode;selectedKey:string;children:React.ReactNode}){const rootSelected=selectedKey===root.key;return <div className="grid min-h-[calc(100dvh-14rem)] overflow-hidden rounded-2xl border bg-white shadow-sm lg:grid-cols-[360px_minmax(0,1fr)]"><aside className={`${rootSelected?"block":"hidden"} min-h-[32rem] border-r lg:block`}><ContentStudioTree bookId={bookId} root={root} selectedKey={selectedKey}/></aside><section className={`${rootSelected?"hidden":"block"} min-w-0 overflow-y-auto bg-slate-50/60 p-4 sm:p-6 lg:block`}><Link href={`/admin/books/${bookId}/content`} className="mb-4 inline-flex rounded-lg border bg-white px-3 py-2 text-sm font-semibold text-blue-700 lg:hidden">← Back to Tree</Link>{children}</section></div>}
