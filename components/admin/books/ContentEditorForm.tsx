"use client";
import{useEffect,useRef,useState}from"react";
export default function ContentEditorForm({action,children,submitLabel="Save changes",className="space-y-5"}:{action:(data:FormData)=>Promise<void>;children:React.ReactNode;submitLabel?:string;className?:string}){const formRef=useRef<HTMLFormElement>(null),[dirty,setDirty]=useState(false),[saving,setSaving]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");
  useEffect(()=>{const warn=(event:BeforeUnloadEvent)=>{if(!dirty)return;event.preventDefault();event.returnValue=""};window.addEventListener("beforeunload",warn);return()=>window.removeEventListener("beforeunload",warn)},[dirty]);
  async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();if(saving)return;setSaving(true);setError("");setMessage("");try{await action(new FormData(event.currentTarget));setDirty(false);setMessage("Changes saved.")}catch(cause){setError(cause instanceof Error?cause.message:"Unable to save changes.")}finally{setSaving(false)}}
  return <form ref={formRef} onSubmit={submit} onChange={()=>{setDirty(true);setMessage("");setError("")}} data-content-editor-dirty={dirty?"true":"false"} className={className}>{children}{error?<p role="alert" className="rounded-xl bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</p>:null}{message?<p role="status" className="rounded-xl bg-emerald-50 p-3 text-sm font-semibold text-emerald-700">{message}</p>:null}<div className="sticky bottom-0 flex items-center justify-end gap-3 border-t bg-white/95 py-4"><span className="mr-auto text-xs font-semibold text-slate-500">{dirty?"Unsaved changes":"All changes saved"}</span><button type="button" disabled={!dirty||saving} onClick={()=>{formRef.current?.reset();setDirty(false);setError("");setMessage("Changes discarded.")}} className="rounded-xl border px-4 py-2 font-semibold disabled:opacity-40">Discard</button><button disabled={!dirty||saving} className="rounded-xl bg-blue-600 px-5 py-2 font-semibold text-white disabled:opacity-40">{saving?"Saving…":submitLabel}</button></div></form>}
