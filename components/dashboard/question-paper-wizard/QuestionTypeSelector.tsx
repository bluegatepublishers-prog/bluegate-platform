export const QUESTION_TYPES=["MCQ","Very Short","Short","Long","Case Study","Assertion Reason","Competency Based","HOTS"];
export default function QuestionTypeSelector({ selected, counts, auto, onToggle, onCount }: {selected:string[];counts:Record<string,number>;auto:boolean;onToggle:(v:string)=>void;onCount:(v:string,n:number)=>void}) { return <div className="grid gap-3 sm:grid-cols-2">{QUESTION_TYPES.map(type=><div key={type} className={`flex items-center gap-3 rounded-xl border p-4 ${selected.includes(type)?"border-blue-400 bg-blue-50":""}`}><label className="flex flex-1 cursor-pointer items-center gap-3 font-semibold"><input type="checkbox" checked={selected.includes(type)} onChange={()=>onToggle(type)} className="h-5 w-5 accent-blue-600"/>{type}</label>{!auto&&selected.includes(type)&&<input aria-label={`${type} question count`} type="number" min="1" max="50" value={counts[type]??1} onChange={e=>onCount(type,Number(e.target.value))} className="w-20 rounded-lg border bg-white px-3 py-2"/>}</div>)}</div>; }
