import { Crown, Sparkles } from "lucide-react"; import type { PaperSettingsValue, WizardBook } from "./types";
export default function ReviewPanel({ book, chapterIds, coverage, settings, entitlement }: {book:WizardBook;chapterIds:string[];coverage:"whole"|"selected";settings:PaperSettingsValue;entitlement:{plan:string;remaining:number;limit:number;canGenerate:boolean}}) { const chapters=book.chapters.filter(c=>chapterIds.includes(c.id)); const estimated=settings.autoDistribution?Math.max(settings.questionTypes.length,Math.ceil(settings.totalMarks/2)):settings.questionTypes.reduce((n,t)=>n+(settings.questionCounts[t]??1),0); return <div className="space-y-5"><div className="rounded-3xl bg-gradient-to-br from-blue-600 to-indigo-700 p-6 text-white sm:p-8"><p className="text-sm font-bold uppercase tracking-widest text-blue-100">Ready to create</p><h3 className="mt-2 text-2xl font-bold">{settings.title}</h3><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><Card label="Book" value={book.title}/><Card label="Coverage" value={coverage==="whole"?`Whole book · ${chapters.length} AI Ready chapters`:`${chapters.length} selected chapters`}/><Card label="Exam type" value={`${settings.examType} · ${settings.pattern}`}/><Card label="Marks and duration" value={`${settings.totalMarks} marks · ${settings.duration} minutes`}/><Card label="Difficulty" value={settings.difficulty}/><Card label="Distribution" value={settings.autoDistribution?"Automatic":settings.questionTypes.join(", ")}/><Card label="Estimated questions" value={String(estimated)}/></div></div><div className={`rounded-2xl border p-5 ${entitlement.canGenerate?"border-emerald-200 bg-emerald-50":"border-amber-200 bg-amber-50"}`}><div className="flex items-center gap-3"><Crown className="h-6 w-6 text-amber-600"/><div><p className="font-bold">{entitlement.plan} plan</p><p className="text-sm text-slate-600">{entitlement.remaining} of {entitlement.limit} daily generations remaining</p></div></div></div><p className="flex items-center gap-2 text-sm text-slate-500"><Sparkles className="h-4 w-4"/>Future actions: assign to class, save as template, share with school, and export.</p></div>; }
function Card({label,value}:{label:string;value:string}){return <div className="rounded-2xl bg-white/10 p-4 backdrop-blur"><p className="text-xs font-bold uppercase tracking-wider text-blue-100">{label}</p><p className="mt-2 font-semibold text-white">{value}</p></div>}
