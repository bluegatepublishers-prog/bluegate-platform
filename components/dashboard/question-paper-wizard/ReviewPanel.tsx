import { Crown, Sparkles } from "lucide-react"; import type { PaperSettingsValue, WizardBook } from "./types";
export default function ReviewPanel({ book, chapterIds, settings, entitlement }: {book:WizardBook;chapterIds:string[];settings:PaperSettingsValue;entitlement:{plan:string;remaining:number;limit:number;canGenerate:boolean}}) { const chapters=book.chapters.filter(c=>chapterIds.includes(c.id)); const estimated=settings.autoDistribution?Math.max(settings.questionTypes.length,Math.ceil(settings.totalMarks/2)):settings.questionTypes.reduce((n,t)=>n+(settings.questionCounts[t]??1),0); return <div className="space-y-5"><div className="grid gap-4 sm:grid-cols-2"><Card label="Book" value={book.title}/><Card label="Chapters" value={chapters.map(c=>c.title).join(", ")}/><Card label="Marks and duration" value={`${settings.totalMarks} marks · ${settings.duration} minutes`}/><Card label="Difficulty" value={settings.difficulty}/><Card label="Question types" value={settings.questionTypes.join(", ")}/><Card label="Estimated questions" value={String(estimated)}/></div><div className={`rounded-2xl border p-5 ${entitlement.canGenerate?"border-emerald-200 bg-emerald-50":"border-amber-200 bg-amber-50"}`}><div className="flex items-center gap-3"><Crown className="h-6 w-6 text-amber-600"/><div><p className="font-bold">{entitlement.plan} plan</p><p className="text-sm text-slate-600">{entitlement.remaining} of {entitlement.limit} daily AI generations remaining</p></div></div></div><p className="flex items-center gap-2 text-sm text-slate-500"><Sparkles className="h-4 w-4"/>Your paper will be grounded only in approved chapter content.</p></div>; }
function Card({label,value}:{label:string;value:string}){return <div className="rounded-2xl bg-slate-50 p-5"><p className="text-xs font-bold uppercase tracking-wider text-slate-500">{label}</p><p className="mt-2 font-semibold text-slate-900">{value}</p></div>}
