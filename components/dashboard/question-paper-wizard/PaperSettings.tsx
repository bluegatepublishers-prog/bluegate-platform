import QuestionTypeSelector from "./QuestionTypeSelector"; import type { PaperSettingsValue } from "./types";
const input="mt-2 w-full rounded-xl border border-slate-300 bg-white px-4 py-3";
export default function PaperSettings({ value, onChange }: {value:PaperSettingsValue;onChange:(v:PaperSettingsValue)=>void}) { return <div className="space-y-7"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><label className="text-sm font-semibold sm:col-span-2">Paper title<input className={input} maxLength={160} value={value.title} onChange={e=>onChange({...value,title:e.target.value})}/></label><label className="text-sm font-semibold">Total marks<input className={input} type="number" min="1" max="500" value={value.totalMarks} onChange={e=>onChange({...value,totalMarks:Number(e.target.value)})}/></label><label className="text-sm font-semibold">Duration (minutes)<input className={input} type="number" min="10" max="360" value={value.duration} onChange={e=>onChange({...value,duration:Number(e.target.value)})}/></label></div><div><p className="mb-3 text-sm font-semibold">Difficulty</p><div className="flex flex-wrap gap-3">{(["Easy","Medium","Hard"] as const).map(x=><button type="button" key={x} onClick={()=>onChange({...value,difficulty:x})} className={`rounded-xl px-5 py-3 font-semibold ${value.difficulty===x?"bg-blue-600 text-white":"border bg-white"}`}>{x}</button>)}</div></div><label className="flex items-center gap-3 rounded-xl bg-slate-50 p-4 font-semibold"><input type="checkbox" checked={value.autoDistribution} onChange={e=>onChange({...value,autoDistribution:e.target.checked})} className="h-5 w-5 accent-blue-600"/>Automatically distribute questions</label><QuestionTypeSelector selected={value.questionTypes} counts={value.questionCounts} auto={value.autoDistribution} onToggle={type=>onChange({...value,questionTypes:value.questionTypes.includes(type)?value.questionTypes.filter(x=>x!==type):[...value.questionTypes,type]})} onCount={(type,count)=>onChange({...value,questionCounts:{...value.questionCounts,[type]:count}})}/></div>; }
