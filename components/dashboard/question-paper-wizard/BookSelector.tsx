import Image from "next/image";
import type { WizardBook } from "./types";
const input = "w-full rounded-xl border border-slate-300 bg-white px-4 py-3 outline-none focus:border-blue-500";
export default function BookSelector({ classes, subjects, books, classId, subjectId, bookId, search, onClass, onSubject, onBook, onSearch }: { classes: {id:string;name:string}[]; subjects:{id:string;name:string}[]; books:WizardBook[]; classId:string;subjectId:string;bookId:string;search:string;onClass:(v:string)=>void;onSubject:(v:string)=>void;onBook:(v:string)=>void;onSearch:(v:string)=>void }) { const filtered=books.filter(b=>(!classId||b.classId===classId)&&(!subjectId||b.subjectId===subjectId)&&(!search||`${b.title} ${b.series??""}`.toLowerCase().includes(search.toLowerCase()))); const selected=books.find(b=>b.id===bookId); return <div className="space-y-6"><div className="grid gap-4 md:grid-cols-3"><label className="text-sm font-semibold">Class<select className={`${input} mt-2`} value={classId} onChange={e=>onClass(e.target.value)}><option value="">Select class</option>{classes.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label className="text-sm font-semibold">Subject<select className={`${input} mt-2`} value={subjectId} onChange={e=>onSubject(e.target.value)}><option value="">Select subject</option>{subjects.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label className="text-sm font-semibold">Search books<input className={`${input} mt-2`} value={search} onChange={e=>onSearch(e.target.value)} placeholder="Book or series name"/></label></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{filtered.map(book=><button type="button" key={book.id} onClick={()=>onBook(book.id)} className={`rounded-2xl border p-4 text-left transition ${book.id===bookId?"border-blue-500 bg-blue-50 ring-2 ring-blue-100":"hover:border-slate-400"}`}><p className="font-bold">{book.title}</p><p className="mt-1 text-sm text-slate-500">{book.series??"Bluegate"} · {book.className} · {book.subjectName}</p></button>)}</div>{selected&&<div className="flex flex-col gap-5 rounded-2xl bg-slate-50 p-5 sm:flex-row"><div className="relative h-40 w-28 shrink-0 overflow-hidden rounded-xl bg-slate-200">{selected.coverImage?<Image src={selected.coverImage} alt="" fill className="object-cover" sizes="112px" unoptimized/>:<div className="flex h-full items-center justify-center p-3 text-center text-xs text-slate-500">No cover</div>}</div><div><p className="text-xs font-bold uppercase tracking-wider text-blue-700">Selected book</p><h3 className="mt-1 text-xl font-bold">{selected.title}</h3><p className="mt-2 text-sm text-slate-600">Series: {selected.series??"Not specified"}<br/>Publisher: {selected.publisher??"Bluegate"}</p><p className="mt-3 text-sm text-slate-600">{selected.summary??"Approved Bluegate book knowledge is available chapter by chapter."}</p></div></div>}</div>; }
