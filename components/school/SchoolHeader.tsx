import LogoutButton from "@/components/dashboard/LogoutButton";
export default function SchoolHeader({name}:{name:string}) { return <header className="sticky top-0 z-30 hidden h-16 items-center justify-between border-b border-slate-200 bg-white px-8 lg:flex"><div><p className="text-sm font-bold text-slate-900">School dashboard</p><p className="mt-0.5 text-sm text-slate-500">{name}</p></div><LogoutButton /></header>; }
