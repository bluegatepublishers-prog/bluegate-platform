import { unstable_rethrow } from "next/navigation";
import { NextResponse } from "next/server";
import { answerStudentPractice, practiceErrorResponse } from "@/lib/student-practice";
export async function PATCH(request: Request, { params }: { params: Promise<{ attemptId: string }> }) { try { const [{ attemptId }, body] = await Promise.all([params, request.json() as Promise<{ questionId?: unknown; answer?: unknown }>]); if (typeof body.questionId !== "string") throw new Error("invalid"); const result = await answerStudentPractice({ attemptId, questionId: body.questionId, answer: body.answer }); return NextResponse.json({ ok: true, ...result }); } catch (error) { unstable_rethrow(error); const safe = practiceErrorResponse(error); return NextResponse.json({ ok: false, message: safe.message }, { status: safe.status }); } }
