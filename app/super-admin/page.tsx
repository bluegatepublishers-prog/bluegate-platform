import{prisma}from"@/lib/prisma";import{requireSuperAdmin}from"@/lib/publisher-context";export default async function Page(){await requireSuperAdmin();const[publishers,activePublishers,schools,books,platformFeatures,enabledPublisherFeatures]=await Promise.all([prisma.publisher.count(),prisma.publisher.count({where:{active:true}}),prisma.school.count(),prisma.book.count(),prisma.featureDefinition.count({where:{active:true,implemented:true}}),prisma.publisherFeature.count({where:{enabled:true,feature:{active:true,implemented:true}}})]);return <div><h1 className="text-3xl font-bold">Platform overview</h1><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{[["Publishers",publishers],["Active publishers",activePublishers],["Suspended publishers",publishers-activePublishers],["Schools",schools],["Books",books],["Platform-ready features",platformFeatures],["Enabled publisher features",enabledPublisherFeatures]].map(([label,value])=><div key={label} className="rounded-2xl border bg-white p-6"><p className="text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></div>)}</div></div>}
