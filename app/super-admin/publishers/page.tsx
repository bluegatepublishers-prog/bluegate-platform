import Link from"next/link";import{prisma}from"@/lib/prisma";import{requireSuperAdmin}from"@/lib/publisher-context";import PublisherLifecycleButton from"@/components/super-admin/PublisherLifecycleButton";import{reactivatePublisher,suspendPublisher}from"./management-actions";export default async function Page(){await requireSuperAdmin();const rows=await prisma.publisher.findMany({include:{_count:{select:{schools:true,books:true}},features:{where:{enabled:true,feature:{active:true,implemented:true}},select:{id:true}}},orderBy:{createdAt:"asc"}});return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-blue-700">Edora platform operations</p><h1 className="mt-2 text-3xl font-bold">Publishers</h1><p className="mt-2 text-slate-600">Manage publisher tenants, access state, and explicit feature permissions.</p></div><Link href="/super-admin/publishers/new" className="rounded-xl bg-slate-950 px-5 py-3 font-bold text-white">+ Add Publisher</Link></div><div className="mt-5 rounded-2xl border border-blue-100 bg-blue-50 p-4 text-sm text-blue-900">Publisher lifecycle uses the existing <strong>active</strong> field. Suspend blocks dependent access while preserving data; Reactivate restores access. Archive and hard delete are not available because no safe persisted archive state exists.</div><div className="mt-6 overflow-x-auto rounded-2xl border bg-white"><table className="w-full"><thead><tr className="text-left"><th className="p-4">Name</th><th>Slug</th><th>Status</th><th>Schools</th><th>Books</th><th>Features</th><th>Created</th><th className="pr-4">Actions</th></tr></thead><tbody>{rows.map(x=><tr key={x.id} className="border-t"><td className="p-4"><Link className="font-semibold text-blue-700" href={`/super-admin/publishers/${x.id}`}>{x.name}</Link></td><td>{x.slug}</td><td>{x.active?"Active":"Suspended"}</td><td>{x._count.schools}</td><td>{x._count.books}</td><td>{x.features.length}</td><td>{x.createdAt.toLocaleDateString("en-IN")}</td><td className="pr-4"><div className="flex flex-wrap gap-2"><Link href={`/super-admin/publishers/${x.id}`} className="rounded-xl border border-slate-200 px-3 py-2 text-xs font-bold text-slate-700">Open</Link>{x.active?<PublisherLifecycleButton action={suspendPublisher.bind(null,x.id)} label="Suspend" confirmMessage="Suspend this Publisher? This will temporarily block the Publisher and dependent access while preserving data." tone="danger"/>:<PublisherLifecycleButton action={reactivatePublisher.bind(null,x.id)} label="Reactivate" confirmMessage="Reactivate this Publisher and restore dependent access?"/>}</div></td></tr>)}</tbody></table></div></div>}
