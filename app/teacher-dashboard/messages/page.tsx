import Link from "next/link";
import { MessageCircle } from "lucide-react";
import { getTeacherMessageRooms } from "@/lib/teacher-class-chat";

export default async function TeacherMessagesPage() { const rooms = await getTeacherMessageRooms(); return <main className="space-y-6 p-4 sm:p-8"><section className="rounded-3xl border border-slate-100 bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">Assigned Class Rooms</h2>{rooms.length ? <div className="mt-5 divide-y">{rooms.map((room) => <Link key={room.id} href={`/teacher-dashboard/classes/${room.sectionId}/chat`} className="flex items-center gap-4 py-4"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-blue-50 text-blue-600"><MessageCircle className="h-5 w-5" /></span><div className="min-w-0 flex-1"><strong>{room.section.schoolClass.name}-{room.section.name}</strong><p className="mt-1 truncate text-sm text-slate-500">{room.messages[0] ? `${room.messages[0].sender.name}: ${room.messages[0].text}` : "No messages yet"}</p></div>{room.unread > 0 && <span className="rounded-full bg-red-500 px-2 py-1 text-xs font-bold text-white">{room.unread}</span>}</Link>)}</div> : <p className="mt-6 rounded-2xl bg-slate-50 p-10 text-center text-slate-500">No active class rooms yet.</p>}</section></main>; }
