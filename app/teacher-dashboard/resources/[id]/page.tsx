import Link from "next/link";
import { ArrowLeft, FileText } from "lucide-react";
import ResourceActions from "@/components/dashboard/ResourceActions";
import { getResourceDetails } from "@/lib/teacher-dashboard";
export const dynamic="force-dynamic"; export const revalidate=0;
export default async function ResourceDetailsPage({params}:{params:Promise<{id:string}>}) { const {id}=await params; const {resource,related,bookmarked}=await getResourceDetails(id); const video=resource.type==="VIDEO"; const pdf=resource.type==="PDF"; return <div className="space-y-8 p-4 sm:p-6 lg:p-8"><Link href="/teacher-dashboard/resources" className="inline-flex items-center font-semibold text-blue-700"><ArrowLeft className="mr-2 h-4 w-4"/>Back to resources</Link><div className="grid gap-8 xl:grid-cols-[1.5fr_1fr]"><section className="overflow-hidden rounded-3xl border bg-white shadow-sm">{video?<video controls className="aspect-video w-full bg-black" src={resource.fileUrl}>Your browser does not support video.</video>:pdf?<iframe title={`${resource.title} preview`} src={resource.fileUrl} className="h-[70vh] w-full"/>:<div className="flex min-h-96 items-center justify-center bg-slate-50"><div className="text-center"><FileText className="mx-auto h-16 w-16 text-blue-600"/><p className="mt-4 text-slate-600">Preview is available after download for this file type.</p></div></div>}</section><aside className="rounded-3xl border bg-white p-7 shadow-sm"><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-bold text-blue-700">{resource.type}</span><h1 className="mt-5 text-3xl font-bold">{resource.title}</h1><p className="mt-4 leading-7 text-slate-600">{resource.description}</p><div className="mt-5 flex flex-wrap gap-2"><span className="rounded-full bg-slate-100 px-3 py-1">{resource.classLevel}</span><span className="rounded-full bg-slate-100 px-3 py-1">{resource.subject}</span></div><ResourceActions resourceId={resource.id} bookmarked={bookmarked}/></aside></div><section><h2 className="text-2xl font-bold">Related resources</h2>{related.length?<div className="mt-5 grid gap-5 sm:grid-cols-2 xl:grid-cols-4">{related.map(item=><Link key={item.id} href={`/teacher-dashboard/resources/${item.id}`} className="rounded-2xl border bg-white p-5 shadow-sm"><span className="text-xs font-bold text-blue-700">{item.type}</span><h3 className="mt-3 font-bold">{item.title}</h3><p className="mt-2 text-sm text-slate-500">{item.classLevel} · {item.subject}</p></Link>)}</div>:<p className="mt-4 text-slate-500">No related resources available.</p>}</section></div>; }
