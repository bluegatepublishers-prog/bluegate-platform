import type{ReactNode}from"react";import{PlatformFeatureKey}from"@prisma/client";import{requirePublisherContext}from"@/lib/publisher-context";import{requirePublisherFeature}from"@/lib/publisher-features";export default async function Layout({children}:{children:ReactNode}){const{publisher}=await requirePublisherContext();await requirePublisherFeature(publisher.id,PlatformFeatureKey.AI_STUDIO);return children}
