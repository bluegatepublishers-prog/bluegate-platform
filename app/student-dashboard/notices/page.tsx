import Link from "next/link";
import { CalendarDays, Megaphone } from "lucide-react";
import { getStudentNotices } from "@/lib/student-classroom-data";

export default async function NoticesPage({ searchParams }: { searchParams: Promise<{ type?: string; q?: string }> }) { const query = await searchParams; const notices = await getStudentNotices({ type: query.type, query: query.q }); return <main className="space-y-6 p-4 sm:p-8"><header><p className="font-semibold text-blue-600">Official updates</p><h1 className="mt-1 text-3xl font-bold">Notices</h1><p className="mt-2 text-slate-500">School, class, examination, holiday, and event information for you.</p></header><form className="flex flex-col gap-3 rounded-3xl border bg-white p-4 shadow-sm sm:flex-row"><input name="q" defaultValue={query.q} placeholder="Search notices" className="min-w-0 flex-1 rounded-2xl border px-4 py-3 outline-none focus:border-blue-500" /><select name="type" defaultValue={query.type ?? ""} className="rounded-2xl border px-4 py-3"><option value="">All types</option><option value="NOTICE">Notices</option><option value="HOLIDAY">Holidays</option><option value="EMERGENCY_HOLIDAY">Emergency closures</option><option value="EVENT">Events</option></select><button className="rounded-2xl bg-blue-600 px-5 py-3 font-semibold text-white">Filter</button></form>{notices.length ? <section className="grid gap-4">{notices.map((notice) => <article key={notice.id} className="rounded-3xl border border-slate-100 bg-white p-6 shadow-sm"><div className="flex gap-4"><span className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-blue-50 text-blue-600"><Megaphone className="h-5 w-5" /></span><div><span className="text-xs font-bold uppercase tracking-wide text-blue-600">{notice.type}</span><h2 className="mt-1 text-xl font-bold">{notice.title}</h2>{notice.description && <p className="mt-2 text-slate-600">{notice.description}</p>}<p className="mt-3 flex items-center gap-2 text-sm text-slate-400"><CalendarDays className="h-4 w-4" />{notice.currentDate.toLocaleDateString("en-IN", { day: "numeric", month: "long", year: "numeric" })}</p></div></div></article>)}</section> : <section className="rounded-3xl border bg-white p-12 text-center"><Megaphone className="mx-auto h-10 w-10 text-slate-300" /><h2 className="mt-4 text-xl font-bold">No notices found</h2><p className="mt-2 text-slate-500">You are all caught up.</p>{(query.q || query.type) && <Link href="/student-dashboard/notices" className="mt-4 inline-flex font-semibold text-blue-600">Clear filters</Link>}</section>}</main>; }
