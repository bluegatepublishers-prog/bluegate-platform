import Link from "next/link";
import { CalendarDays } from "lucide-react";
import { getStudentPlannerItems } from "@/lib/student-classroom-data";

export default async function PlannerPage() { const items = await getStudentPlannerItems(); const now = new Date(); const month = now.toLocaleDateString("en-IN", { month: "long", year: "numeric" }); return <main className="space-y-6 p-4 sm:p-8"><header><p className="font-semibold text-blue-600">Your academic year</p><h1 className="mt-1 text-3xl font-bold">Planner</h1><p className="mt-2 text-slate-500">Assignments, assessments, teaching plans, events, and revised dates in one place.</p></header><section className="rounded-3xl border border-slate-100 bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><CalendarDays className="h-6 w-6 text-blue-600" /><h2 className="text-xl font-bold">{month}</h2></div>{items.length ? <div className="mt-6 divide-y">{items.map((item) => { const row = <div className="grid gap-3 py-4 sm:grid-cols-[90px_1fr_auto] sm:items-center"><time className="font-bold text-blue-700">{item.date.toLocaleDateString("en-IN", { day: "numeric", month: "short" })}</time><div><div className="flex flex-wrap items-center gap-2"><strong>{item.title}</strong><span className="rounded-full bg-slate-100 px-2 py-1 text-[10px] font-bold uppercase text-slate-500">{item.type}</span>{item.rescheduled && <span className="rounded-full bg-amber-100 px-2 py-1 text-[10px] font-bold uppercase text-amber-700">Rescheduled</span>}</div>{item.description && <p className="mt-1 text-sm text-slate-500">{item.description}</p>}{item.rescheduled && <p className="mt-1 text-xs text-slate-400">Originally {item.originalDate.toLocaleDateString("en-IN")}</p>}</div><span className="text-xs font-semibold text-slate-400">{item.status.replaceAll("_", " ")}</span></div>; return item.href ? <Link key={item.id} href={item.href} className="block hover:bg-slate-50">{row}</Link> : <div key={item.id}>{row}</div>; })}</div> : <div className="py-16 text-center"><CalendarDays className="mx-auto h-10 w-10 text-slate-300" /><h2 className="mt-4 text-xl font-bold">Nothing is planned yet</h2><p className="mt-2 text-slate-500">Published academic items will appear here automatically.</p></div>}</section></main>; }
