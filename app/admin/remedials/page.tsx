import { getAuthenticatedPublisherRemedialAggregate } from "@/lib/remedials/aggregates";
export const dynamic="force-dynamic";export const revalidate=0;
export default async function AdminRemedialsPage(){const r=await getAuthenticatedPublisherRemedialAggregate();return <div className="space-y-7"><header><p className="font-bold text-indigo-700">Aggregate only · no student identities</p><h1 className="text-3xl font-bold">Remedial effectiveness</h1><p className="text-slate-600">Completion and effectiveness use stored, real plan and resolved-gap outcomes. A dash means there is not enough data.</p></header><section className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead><tr>{["Approved resource","Assigned","Completed","Completion","Effectiveness"].map(x=><th key={x} className="p-4">{x}</th>)}</tr></thead><tbody>{r.resources.map((x,i)=><tr key={`${x.label}-${i}`} className="border-t"><td className="p-4 font-semibold">{x.label}</td><td className="p-4">{x.assigned}</td><td className="p-4">{x.completed}</td><td className="p-4">{x.completionRate==null?"—":`${x.completionRate}%`}</td><td className="p-4">{x.effectivenessRate==null?"—":`${x.effectivenessRate}%`}</td></tr>)}</tbody></table></section></div>}
