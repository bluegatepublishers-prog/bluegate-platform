import type{ReactNode}from"react";import{PlatformFeatureKey}from"@prisma/client";import{requirePublisherAdmin}from"@/lib/publisher-context";import{requirePublisherFeature}from"@/lib/publisher-features";export default async function Layout({children}:{children:ReactNode}){const{publisher}=await requirePublisherAdmin();await requirePublisherFeature(publisher.id,PlatformFeatureKey.RESOURCES);return children}
