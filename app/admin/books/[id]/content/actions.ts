"use server";
import{BookPartKind,CurriculumDifficultyLevel,CurriculumExerciseType,Prisma,SecurityAuditOutcome}from"@prisma/client";import{revalidatePath}from"next/cache";
import{moveBookStructureNode,reorderBookStructureNodes,saveBookStructureNode,setBookStructureNodeArchived,type BookStructureNodeType}from"@/lib/book-structure-management";import{prisma}from"@/lib/prisma";import{requireLivePublisherAdmin}from"@/lib/publisher-admin-authorization";import{publisherAdminAuditActor,writeSecurityAuditEvent}from"@/lib/security-audit";
const text=(form:FormData,key:string,max=4000)=>String(form.get(key)??"").trim().slice(0,max),nullable=(form:FormData,key:string,max?:number)=>text(form,key,max)||null,integer=(form:FormData,key:string)=>{const value=Number(form.get(key));return Number.isInteger(value)&&value>=0?value:null};
function refresh(bookId:string){revalidatePath(`/admin/books/${bookId}/content`)}
export async function saveContentNodeAction(bookId:string,type:BookStructureNodeType,id:string,form:FormData){let preserved:{parentId?:string|null;secondaryParentId?:string|null;shortTitle:string|null;code:string|null;content:Prisma.JsonValue;imageUrl:string|null;pageStart?:number|null;pageEnd?:number|null;partKind?:BookPartKind};if(type==="PART"){const row=await prisma.bookPart.findFirst({where:{id,bookId},select:{shortTitle:true,code:true,content:true,imageUrl:true,kind:true}});if(!row)throw new Error("Part not found.");preserved={...row,partKind:row.kind}}else if(type==="UNIT"){const row=await prisma.bookUnit.findFirst({where:{id,bookId},select:{partId:true,shortTitle:true,code:true,content:true,imageUrl:true}});if(!row)throw new Error("Unit not found.");preserved={...row,parentId:row.partId}}else if(type==="CHAPTER"){const row=await prisma.bookChapter.findFirst({where:{id,bookId},select:{unitId:true,partId:true,shortTitle:true,content:true,thumbnail:true,startPage:true,endPage:true}});if(!row)throw new Error("Chapter not found.");preserved={parentId:row.unitId,secondaryParentId:row.partId,shortTitle:row.shortTitle,code:null,content:row.content,imageUrl:row.thumbnail,pageStart:row.startPage,pageEnd:row.endPage}}else if(type==="MODULE"){const row=await prisma.bookModule.findFirst({where:{id,bookId},select:{chapterId:true,shortTitle:true,code:true,content:true,imageUrl:true}});if(!row)throw new Error("Module not found.");preserved={...row,parentId:row.chapterId}}else{const row=await prisma.bookTopic.findFirst({where:{id,bookId},select:{chapterId:true,moduleId:true,shortTitle:true,code:true,content:true,imageUrl:true}});if(!row)throw new Error("Topic not found.");preserved={...row,parentId:row.chapterId,secondaryParentId:row.moduleId}}await saveBookStructureNode(bookId,{type,id,parentId:preserved.parentId,secondaryParentId:preserved.secondaryParentId,title:text(form,"title",200),subtitle:nullable(form,"subtitle",240),shortTitle:preserved.shortTitle,code:preserved.code,slug:nullable(form,"slug",180),label:nullable(form,"label",80),description:nullable(form,"description",2000),content:(preserved.content??Prisma.JsonNull)as Prisma.InputJsonValue,estimatedMinutes:integer(form,"estimatedMinutes"),pageStart:preserved.pageStart,pageEnd:preserved.pageEnd,imageUrl:preserved.imageUrl,published:form.get("published")==="on",partKind:preserved.partKind});refresh(bookId)}
export async function createContentChildAction(bookId:string,parentType:string,parentId:string,form:FormData){const type=text(form,"type")as BookStructureNodeType;const allowed:Record<string,BookStructureNodeType[]>={BOOK:["PART","UNIT","CHAPTER"],PART:["UNIT","CHAPTER"],UNIT:["CHAPTER"],CHAPTER:["MODULE","TOPIC"],MODULE:["TOPIC"]};if(!allowed[parentType]?.includes(type))throw new Error("This child type is not valid for the selected parent.");let parent:string|null=null,secondary:string|null=null;if(type==="UNIT"&&parentType==="PART")parent=parentId;if(type==="CHAPTER"){if(parentType==="UNIT")parent=parentId;else if(parentType==="PART")secondary=parentId}else if(type==="MODULE")parent=parentId;else if(type==="TOPIC"){if(parentType==="MODULE"){const moduleNode=await prisma.bookModule.findFirst({where:{id:parentId,bookId},select:{chapterId:true}});if(!moduleNode)throw new Error("Parent Module not found.");parent=moduleNode.chapterId;secondary=parentId}else parent=parentId}await saveBookStructureNode(bookId,{type,parentId:parent,secondaryParentId:secondary,title:text(form,"title",200),label:nullable(form,"label",80),description:nullable(form,"description",2000),published:false});refresh(bookId)}
export async function archiveContentNodeAction(bookId:string,type:BookStructureNodeType,id:string,archived:boolean){await setBookStructureNodeArchived(bookId,type,id,archived);refresh(bookId)}
export async function reorderContentNodeAction(bookId:string,type:BookStructureNodeType,id:string,direction:-1|1){const rows=type==="PART"?await prisma.bookPart.findMany({where:{bookId},select:{id:true},orderBy:[{displayOrder:"asc"},{id:"asc"}]}):type==="UNIT"?await prisma.bookUnit.findMany({where:{bookId},select:{id:true},orderBy:[{displayOrder:"asc"},{id:"asc"}]}):type==="CHAPTER"?await prisma.bookChapter.findMany({where:{bookId},select:{id:true},orderBy:[{sortOrder:"asc"},{id:"asc"}]}):type==="MODULE"?await prisma.bookModule.findMany({where:{bookId},select:{id:true},orderBy:[{displayOrder:"asc"},{id:"asc"}]}):await prisma.bookTopic.findMany({where:{bookId},select:{id:true},orderBy:[{displayOrder:"asc"},{id:"asc"}]});const index=rows.findIndex(row=>row.id===id),next=index+direction;if(index<0||next<0||next>=rows.length)return;[rows[index],rows[next]]=[rows[next],rows[index]];await reorderBookStructureNodes(bookId,type,rows.map(row=>row.id));refresh(bookId)}
export async function moveContentNodeAction(bookId:string,type:Exclude<BookStructureNodeType,"PART">,id:string,form:FormData){await moveBookStructureNode(bookId,type,id,nullable(form,"parentId"),nullable(form,"secondaryParentId"));refresh(bookId)}
export async function saveChapterKnowledgeAction(bookId:string,chapterId:string,form:FormData){const actor=await requireLivePublisherAdmin();await prisma.$transaction(async tx=>{const chapter=await tx.bookChapter.findFirst({where:{id:chapterId,bookId,book:{publisherId:actor.publisherId}},select:{id:true}});if(!chapter)throw new Error("Chapter not found.");await tx.bookChapter.update({where:{id:chapterId},data:{summary:nullable(form,"summary",12000),extractedText:nullable(form,"extractedText",100000),reviewedText:nullable(form,"reviewedText",100000),keywords:text(form,"keywords",4000).split(",").map(value=>value.trim()).filter(Boolean),approved:form.get("approved")==="on"}});await writeSecurityAuditEvent(tx,{actor:publisherAdminAuditActor(actor),action:"publisher.curriculum.chapter.update",targetType:"BookChapter",targetId:chapterId,outcome:SecurityAuditOutcome.SUCCESS,metadata:{changedFields:["knowledgeContent","reviewState"]}})},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable});refresh(bookId)}
async function ownedChapter(bookId:string,chapterId:string){const actor=await requireLivePublisherAdmin();const chapter=await prisma.bookChapter.findFirst({where:{id:chapterId,bookId,book:{publisherId:actor.publisherId}},select:{id:true}});if(!chapter)throw new Error("Chapter not found.");return actor}
export async function saveExerciseAction(bookId:string,chapterId:string,form:FormData){const actor=await ownedChapter(bookId,chapterId),id=nullable(form,"id"),title=text(form,"title",200),type=text(form,"type")as CurriculumExerciseType,difficulty=nullable(form,"difficulty")as CurriculumDifficultyLevel|null;if(!title||!Object.values(CurriculumExerciseType).includes(type))throw new Error("Exercise title and type are required.");const instructionsText=text(form,"instructions",12000);await prisma.$transaction(async tx=>{const data={title,type,difficulty,marks:integer(form,"marks"),estimatedMinutes:integer(form,"estimatedMinutes"),displayOrder:integer(form,"displayOrder")??0,published:form.get("published")==="on",archived:form.get("archived")==="on",instructions:instructionsText?({text:instructionsText}as Prisma.InputJsonValue):Prisma.JsonNull};let exercise;if(id){const current=await tx.bookExercise.findFirst({where:{id,bookId,chapterId},select:{id:true}});if(!current)throw new Error("Exercise not found.");exercise=await tx.bookExercise.update({where:{id},data})}else exercise=await tx.bookExercise.create({data:{...data,bookId,chapterId}});await writeSecurityAuditEvent(tx,{actor:publisherAdminAuditActor(actor),action:id?"publisher.curriculum.exercise.update":"publisher.curriculum.exercise.create",targetType:"BookExercise",targetId:exercise.id,outcome:SecurityAuditOutcome.SUCCESS,metadata:{changedFields:["exerciseContent"]}})});refresh(bookId)}
export async function archiveExerciseAction(bookId:string,chapterId:string,id:string,archived:boolean){const actor=await ownedChapter(bookId,chapterId);await prisma.$transaction(async tx=>{const result=await tx.bookExercise.updateMany({where:{id,bookId,chapterId},data:{archived,published:archived?false:undefined}});if(!result.count)throw new Error("Exercise not found.");await writeSecurityAuditEvent(tx,{actor:publisherAdminAuditActor(actor),action:archived?"publisher.curriculum.exercise.archive":"publisher.curriculum.exercise.restore",targetType:"BookExercise",targetId:id,outcome:SecurityAuditOutcome.SUCCESS,metadata:{changedFields:["archived","publicationState"]}})});refresh(bookId)}
