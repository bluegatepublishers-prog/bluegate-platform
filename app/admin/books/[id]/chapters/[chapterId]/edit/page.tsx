import{notFound}from"next/navigation";import{prisma}from"@/lib/prisma";import{ChapterForm}from"@/components/admin/BookKnowledgeForm";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string;chapterId:string}>}){const{id,chapterId}=await params;const chapter=await prisma.bookChapter.findFirst({where:{id:chapterId,bookId:id}});if(!chapter)notFound();return <div className="space-y-6"><h2 className="text-2xl font-bold">Edit chapter</h2><ChapterForm bookId={id} chapter={chapter}/></div>}
