import{requirePublisherAdminBookOwnership}from"@/lib/publisher-admin-data";
import{notFound}from"next/navigation";import{prisma}from"@/lib/prisma";import{QuestionForm}from"@/components/admin/BookKnowledgeForm";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string;questionId:string}>}){const{id,questionId}=await params;await requirePublisherAdminBookOwnership(id);const[question,chapters]=await Promise.all([prisma.bookQuestion.findFirst({where:{id:questionId,bookId:id}}),prisma.bookChapter.findMany({where:{bookId:id},select:{id:true,chapterNumber:true,title:true},orderBy:{sortOrder:"asc"}})]);if(!question)notFound();return <div className="space-y-6"><h2 className="text-2xl font-bold">Edit question</h2><QuestionForm bookId={id} chapters={chapters} question={question}/></div>}
