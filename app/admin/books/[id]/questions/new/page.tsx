import{prisma}from"@/lib/prisma";import{QuestionForm}from"@/components/admin/BookKnowledgeForm";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const chapters=await prisma.bookChapter.findMany({where:{bookId:id},select:{id:true,chapterNumber:true,title:true},orderBy:{sortOrder:"asc"}});return <div className="space-y-6"><h2 className="text-2xl font-bold">Add question</h2>{chapters.length?<QuestionForm bookId={id} chapters={chapters}/>:<div className="rounded-2xl border bg-white p-8">Add a chapter before creating questions.</div>}</div>}
