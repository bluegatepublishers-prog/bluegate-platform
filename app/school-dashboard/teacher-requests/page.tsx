import { getSchoolTeacherRequests } from "@/lib/onboarding-approvals";import { reviewTeacherAction } from "./actions";
export const dynamic="force-dynamic";export const revalidate=0;
export default async function TeacherRequestsPage(){const requests=await getSchoolTeacherRequests();return <main className="space-y-7 p-4 sm:p-6 lg:p-8"><header><p className="font-bold text-blue-700">School approval</p><h1 className="text-3xl font-bold">Teacher Requests</h1><p className="mt-2 text-slate-600">Approval links the teacher to this school. Class and subject assignments remain a separate step.</p></header><div className="grid gap-5 xl:grid-cols-2">{requests.map(request=><article key={request.id} className="rounded-3xl border bg-white p-6 shadow-sm"><div className="flex justify-between gap-4"><div><h2 className="text-xl font-bold">{request.teacher.user.name}</h2><p className="text-sm text-slate-600">{request.teacher.user.email} · {request.teacher.designation}</p><p className="text-sm text-slate-500">{request.teacher.subject} · {request.teacher.classes}</p></div><span className="h-fit rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{request.status}</span></div><form action={reviewTeacherAction} className="mt-5 grid gap-3"><input type="hidden" name="requestId" value={request.id}/><label className="text-sm font-semibold">Reason for rejection or suspension<textarea name="reason" className="mt-2 min-h-20 w-full rounded-xl border p-3" maxLength={500}/></label><div className="flex flex-wrap gap-2"><button name="status" value="APPROVED" className="rounded-xl bg-green-700 px-4 py-2 font-bold text-white">Approve</button><button name="status" value="REJECTED" className="rounded-xl border border-rose-300 px-4 py-2 font-bold text-rose-700">Reject</button><button name="status" value="SUSPENDED" className="rounded-xl bg-slate-900 px-4 py-2 font-bold text-white">Suspend</button></div></form></article>)}</div></main>}
