import type{ReactNode}from"react";import SchoolHeader from"@/components/school/SchoolHeader";import SchoolNavigation from"@/components/school/SchoolNavigation";import{requireSchool}from"@/lib/school-dashboard";import{requireUser}from"@/lib/authz";import{getBrandingForAuthenticatedUser}from"@/lib/publisher-context";import{resolveFeaturesForAuthenticatedUser}from"@/lib/publisher-features";import { schoolLogoPath } from "@/lib/storage/media-path";export const dynamic="force-dynamic";
export default async function SchoolLayout({children}:{children:ReactNode}){if(!process.env.DATABASE_URL){await requireUser(["SCHOOL"]);return <div className="min-h-screen bg-slate-100 p-8">Database configuration required</div>}const school=await requireSchool();const[branding,features]=await Promise.all([getBrandingForAuthenticatedUser(),resolveFeaturesForAuthenticatedUser()]);const logoUrl=schoolLogoPath(school.logoUrl);return <div className="flex min-h-screen bg-slate-100"><SchoolNavigation schoolName={school.schoolName} logoUrl={logoUrl} branding={branding} features={features}/><div className="min-w-0 flex-1"><SchoolHeader name={school.user.name}/><SchoolNavigation mobile schoolName={school.schoolName} logoUrl={logoUrl} branding={branding} features={features}/>{children}</div></div>}
