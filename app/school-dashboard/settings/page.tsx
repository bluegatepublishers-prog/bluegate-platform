import Link from "next/link";
import { Palette, Settings, Shield } from "lucide-react";
export default async function SchoolSettingsPage({searchParams}:{searchParams:Promise<{tab?:string}>}){const{tab}=await searchParams;const active=tab==="branding"?"Branding":tab==="security"?"Security":"School Settings";const Icon=active==="Branding"?Palette:active==="Security"?Shield:Settings;return <main className="p-4 sm:p-6 lg:p-8"><section className="rounded-2xl border bg-white p-7 shadow-sm"><span className="grid h-12 w-12 place-items-center rounded-xl bg-blue-50 text-blue-600"><Icon/></span><h1 className="mt-4 text-3xl font-bold">{active}</h1><p className="mt-3 max-w-2xl text-slate-600">Core school identity and contact details are managed in the School Profile. Publisher-controlled branding and access policy remain centrally governed.</p><Link href="/school-dashboard/profile" className="mt-6 inline-flex rounded-xl bg-blue-600 px-5 py-3 font-semibold text-white">Open School Profile</Link></section></main>}
