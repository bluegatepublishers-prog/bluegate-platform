import Image from "next/image";import Link from "next/link";import{BookOpenCheck,GraduationCap,School,UserPlus,UserRoundCheck,Users}from"lucide-react";import{getSchoolDashboard}from"@/lib/school-dashboard";
export const dynamic="force-dynamic";export const revalidate=0;
export default async function SchoolDashboardPage(){const{school,currentYear,stats}=await getSchoolDashboard();return <main className="space-y-8 p-4 sm:p-6 lg:p-8"><section className="flex flex-col gap-6 rounded-3xl bg-gradient-to-br from-blue-700 to-slate-900 p-8 text-white shadow-xl sm:flex-row sm:items-center">{school.logoUrl?<Image src={school.logoUrl} alt="School logo" width={112} height={112} className="h-28 w-28 rounded-2xl bg-white object-contain p-2"/>:<div className="flex h-28 w-28 items-center justify-center rounded-2xl bg-white/15 text-3xl font-bold">{initials(school.schoolName)}</div>}<div><p className="font-semibold text-blue-100">Welcome</p><h1 className="mt-2 text-3xl font-bold sm:text-4xl">{school.schoolName}</h1><p className="mt-3 text-blue-100">Current Academic Year: <strong>{currentYear?.name??"Not selected"}</strong></p></div></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3"><Stat icon={Users} label="Total Teachers" value={stats.teachers}/><Stat icon={GraduationCap} label="Total Students" value={stats.students}/><Stat icon={School} label="Total Classes" value={stats.classes}/><Stat icon={BookOpenCheck} label="Total Sections" value={stats.sections}/><Stat icon={UserRoundCheck} label="Pending Class Teachers" value={stats.pendingClassTeachers} warning/><Stat icon={UserRoundCheck} label="Pending Subject Teachers" value={stats.pendingSubjectTeachers} warning/></section><section className="rounded-3xl border bg-white p-6 shadow-sm"><h2 className="text-2xl font-bold">Quick Actions</h2><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><Action href="/school-dashboard/teachers" label="Add Teacher" icon={UserPlus}/><Action href="/school-dashboard/students" label="Add Student" icon={GraduationCap}/><Action href="/school-dashboard/classes" label="Manage Classes" icon={School}/><Action href="/school-dashboard/teacher-assignments" label="Assign Teachers" icon={UserRoundCheck}/><Action href="/school-dashboard/profile" label="School Profile" icon={School}/></div></section></main>}
function Stat({icon:Icon,label,value,warning=false}:{icon:typeof Users;label:string;value:number;warning?:boolean}){return <div className={`rounded-3xl border bg-white p-6 shadow-sm ${warning&&value>0?"border-amber-300":""}`}><Icon className={`h-9 w-9 ${warning&&value>0?"text-amber-600":"text-blue-700"}`}/><p className="mt-5 text-3xl font-bold">{value}</p><p className="mt-1 text-slate-600">{label}</p></div>}
function Action({href,label,icon:Icon}:{href:string;label:string;icon:typeof Users}){return <Link href={href} className="flex items-center gap-3 rounded-2xl border p-4 font-semibold text-slate-700 hover:border-blue-300 hover:bg-blue-50"><Icon className="h-5 w-5 text-blue-700"/>{label}</Link>}
function initials(name:string){return name.split(/\s+/).filter(Boolean).slice(0,2).map(word=>word[0]?.toUpperCase()).join("")||"S"}
