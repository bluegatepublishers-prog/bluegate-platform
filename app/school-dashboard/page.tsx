import { BookOpen, ClipboardCheck, School, Users } from "lucide-react";
import { getSchoolDashboard } from "@/lib/school-dashboard";
export const dynamic="force-dynamic"; export const revalidate=0;
export default async function SchoolDashboardPage(){const {school,stats}=await getSchoolDashboard();return <div className="space-y-8 p-4 sm:p-6 lg:p-8"><section className="rounded-3xl bg-gradient-to-br from-blue-700 to-slate-900 p-8 text-white shadow-xl"><p className="font-semibold text-blue-100">Welcome</p><h1 className="mt-3 text-3xl font-bold sm:text-4xl">{school.schoolName}</h1><p className="mt-4 flex items-center gap-2 text-blue-100"><School className="h-5 w-5"/>{school.city}, {school.state}</p></section><div className="grid gap-5 sm:grid-cols-3"><Stat icon={Users} label="Teachers" value={stats.teachers}/><Stat icon={BookOpen} label="Available resources" value={stats.resources}/><Stat icon={ClipboardCheck} label="Inspection requests" value={stats.requests}/></div><section className="rounded-3xl border bg-white p-10 text-center shadow-sm"><h2 className="text-2xl font-bold">Recent announcements</h2><p className="mt-3 text-slate-500">No announcements are available.</p></section></div>}
function Stat({icon:Icon,label,value}:{icon:typeof Users;label:string;value:number}){return <div className="rounded-3xl border bg-white p-6 shadow-sm"><Icon className="h-9 w-9 text-blue-700"/><p className="mt-5 text-3xl font-bold">{value}</p><p className="mt-1 text-slate-600">{label}</p></div>}
