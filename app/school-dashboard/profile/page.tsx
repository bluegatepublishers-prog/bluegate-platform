import { Calendar,Mail,MapPin,Phone,School } from "lucide-react";import { requireSchool } from "@/lib/school-dashboard";
export const dynamic="force-dynamic";export const revalidate=0;
export default async function SchoolProfilePage(){const school=await requireSchool();return <div className="space-y-8 p-4 sm:p-6 lg:p-8"><div><h1 className="text-3xl font-bold">School Profile</h1><p className="mt-2 text-slate-600">Account and institution details.</p></div><section className="rounded-3xl bg-gradient-to-r from-blue-700 to-blue-900 p-8 text-white"><School className="h-12 w-12"/><h2 className="mt-5 text-3xl font-bold">{school.schoolName}</h2><p className="mt-2 text-blue-100">{school.city}, {school.state}</p></section><div className="grid gap-6 lg:grid-cols-2"><Card title="School details"><Info icon={School} label="School" value={school.schoolName}/><Info icon={MapPin} label="Location" value={`${school.city}, ${school.state}`}/></Card><Card title="Contact information"><Info icon={Mail} label="Email" value={school.user.email}/><Info icon={Phone} label="Phone" value={school.user.phone??"Not provided"}/><Info icon={Calendar} label="Member since" value={school.user.createdAt.toLocaleDateString("en-IN",{month:"long",year:"numeric"})}/></Card></div></div>}
function Card({title,children}:{title:string;children:React.ReactNode}){return <section className="rounded-3xl border bg-white p-7 shadow-sm"><h2 className="text-2xl font-bold">{title}</h2><div className="mt-6 space-y-5">{children}</div></section>}function Info({icon:Icon,label,value}:{icon:typeof School;label:string;value:string}){return <div className="flex gap-4"><span className="rounded-xl bg-blue-50 p-3 text-blue-700"><Icon className="h-5 w-5"/></span><div><p className="text-sm text-slate-500">{label}</p><p className="mt-1 font-semibold">{value}</p></div></div>}
