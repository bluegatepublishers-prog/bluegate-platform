import SchoolProfileForm from "@/components/school/SchoolProfileForm";import { requireSchool } from "@/lib/school-dashboard";
export const dynamic="force-dynamic";export const revalidate=0;
export default async function SchoolProfilePage(){const school=await requireSchool();return <div className="space-y-8 p-4 sm:p-6 lg:p-8"><div><h1 className="text-3xl font-bold">School Profile</h1><p className="mt-2 text-slate-600">Keep your school and contact details up to date.</p></div><SchoolProfileForm profile={{id:school.id,schoolName:school.schoolName,principalName:school.principalName??"",phone:school.user.phone??"",email:school.user.email,address:school.address??"",city:school.city,state:school.state,pincode:school.pincode??"",logoUrl:school.logoUrl??""}}/></div>}
