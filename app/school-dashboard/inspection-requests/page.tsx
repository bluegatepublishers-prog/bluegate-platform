import { ClipboardCheck } from "lucide-react";import InspectionStatusBadge from "@/components/admin/InspectionStatusBadge";import { getSchoolInspectionRequests } from "@/lib/school-dashboard";
export const dynamic="force-dynamic";export const revalidate=0;
export default async function SchoolInspectionRequestsPage(){const requests=await getSchoolInspectionRequests();return <div className="space-y-8 p-4 sm:p-6 lg:p-8"><div><h1 className="text-3xl font-bold">Inspection Requests</h1><p className="mt-2 text-slate-600">Requests associated with your school account.</p></div>{requests.length?<div className="overflow-x-auto rounded-3xl border bg-white shadow-sm"><table className="w-full min-w-[850px]"><thead className="bg-slate-50 text-left"><tr><th className="p-5">Book</th><th>Teacher</th><th>Class</th><th>Subject</th><th>Status</th><th>Requested</th></tr></thead><tbody>{requests.map(request=><tr key={request.id} className="border-t"><td className="p-5 font-semibold">{request.bookTitle}</td><td>{request.teacherName}</td><td>{request.bookClass}</td><td>{request.bookSubject}</td><td><InspectionStatusBadge status={request.status}/></td><td>{request.createdAt.toLocaleString("en-IN")}</td></tr>)}</tbody></table></div>:<div className="rounded-3xl border bg-white p-14 text-center"><ClipboardCheck className="mx-auto h-12 w-12 text-slate-300"/><h2 className="mt-4 text-xl font-bold">No inspection requests</h2><p className="mt-2 text-slate-500">Requests linked to this school will appear here.</p></div>}</div>}
