import { CalendarDays, Megaphone } from "lucide-react";
import { getSchoolPlannerData } from "@/lib/school-dashboard";
import { createSchoolPlannerItem, rescheduleSchoolPlannerItem, updateSchoolPlannerStatus } from "./actions";
export const dynamic="force-dynamic";
const input="w-full rounded-xl border border-slate-200 px-4 py-3";
export default async function PlannerPage(){const{year,items}=await getSchoolPlannerData();return <main className="space-y-6 p-4 sm:p-6 lg:p-8"><header><h1 className="text-3xl font-bold">Planner</h1><p className="mt-2 text-slate-500">School notices, holidays, events, and the combined academic schedule for {year?.name??"the current year"}.</p></header><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-bold">Add school-wide item</h2><form action={createSchoolPlannerItem} className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-5"><select name="type" className={input} defaultValue="EVENT"><option value="NOTICE">Notice</option><option value="HOLIDAY">Holiday</option><option value="EVENT">Event / PTM / Exam / Assembly</option></select><input name="title" required maxLength={180} placeholder="Title" className={input}/><input name="date" type="datetime-local" required className={input}/><input name="description" placeholder="Details (optional)" className={input}/><button className="rounded-xl bg-blue-600 px-5 py-3 font-bold text-white">Add to planner</button></form><p className="mt-3 text-xs text-slate-500">Holidays are fixed. Events and notices retain a reschedule history; teaching plans remain teacher-owned.</p></section><section className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex items-center gap-3"><CalendarDays className="text-blue-600"/><h2 className="font-bold">Academic timeline</h2></div>{items.length?<div className="mt-5 space-y-3">{items.map(item=><article key={item.id} className="rounded-2xl border p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div className="flex gap-3"><span className={`grid h-10 w-10 place-items-center rounded-xl ${item.type==="NOTICE"?"bg-rose-50 text-rose-600":"bg-blue-50 text-blue-600"}`}><Megaphone className="h-5 w-5"/></span><div><strong>{item.title}</strong><p className="mt-1 text-sm text-slate-500">{item.currentDate.toLocaleString("en-IN",{dateStyle:"medium",timeStyle:"short"})} · {item.type.replaceAll("_"," ")}{item.section?` · ${item.section.schoolClass.name}-${item.section.name}`:" · School-wide"}</p>{item.description&&<p className="mt-2 text-sm text-slate-600">{item.description}</p>}</div></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold">{item.status.replaceAll("_"," ")}</span></div>{item.sectionId==null&&["NOTICE","HOLIDAY","EVENT"].includes(item.type)&&<div className="mt-4 flex flex-wrap gap-3"><form action={updateSchoolPlannerStatus} className="flex gap-2"><input type="hidden" name="id" value={item.id}/><select name="status" defaultValue={item.status} className="rounded-xl border px-3 py-2 text-sm"><option value="PLANNED">Planned</option><option value="IN_PROGRESS">In progress</option><option value="COMPLETED">Completed</option><option value="CANCELLED">Cancelled</option></select><button className="rounded-xl border px-3 py-2 text-sm font-semibold">Update</button></form>{!item.fixedDate&&item.type!=="HOLIDAY"&&<details><summary className="cursor-pointer rounded-xl border px-3 py-2 text-sm font-semibold">Reschedule</summary><form action={rescheduleSchoolPlannerItem} className="mt-2 grid gap-2 rounded-xl bg-slate-50 p-3 sm:grid-cols-3"><input type="hidden" name="id" value={item.id}/><input name="date" type="datetime-local" required className="rounded-lg border px-3 py-2"/><input name="reason" required placeholder="Reason" className="rounded-lg border px-3 py-2"/><button className="rounded-lg bg-slate-900 px-3 py-2 text-sm text-white">Save</button></form></details>}</div>}{item.reschedules.length>0&&<details className="mt-3"><summary className="cursor-pointer text-xs font-semibold text-blue-600">{item.reschedules.length} reschedule record(s)</summary><ul className="mt-2 space-y-1 text-xs text-slate-500">{item.reschedules.map(record=><li key={record.id}>{record.fromDate.toLocaleDateString("en-IN")} → {record.toDate.toLocaleDateString("en-IN")} · {record.reason}</li>)}</ul></details>}</article>)}</div>:<p className="mt-5 rounded-xl bg-slate-50 p-8 text-center text-sm text-slate-500">No planner items yet.</p>}</section></main>}
