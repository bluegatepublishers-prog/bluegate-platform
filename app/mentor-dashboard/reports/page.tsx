import { getMentorReport } from "@/lib/mentor-dashboard";

export default async function MentorReportsPage() {
  const report = await getMentorReport();
  return <main className="space-y-7 p-4 sm:p-6 lg:p-8"><header><p className="font-bold text-indigo-700">Assigned learners only</p><h1 className="mt-2 text-3xl font-bold">Mentor report</h1><p className="mt-2 text-slate-600">Factual current-year summaries. No ranking, prediction, or diagnosis.</p></header><section className="grid gap-4 sm:grid-cols-3">{[["Students assigned",report.studentsAssigned],["Completed remedials",report.completedRemedials],["Open gaps",report.openGaps]].map(([label,value])=><article key={label} className="rounded-2xl border bg-white p-6"><p className="text-3xl font-bold">{value}</p><p className="mt-1 text-slate-600">{label}</p></article>)}</section>{report.rows.length?<section className="grid gap-4 lg:grid-cols-2">{report.rows.map(row=><article key={row.id} className="rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">{row.name}</h2><p className="mt-1 text-sm text-slate-500">{row.school} · {row.className} · Section {row.sectionName}</p><dl className="mt-5 grid grid-cols-2 gap-4 text-sm"><div><dt className="text-slate-500">Learning trend</dt><dd className="font-bold">{row.trend}</dd></div><div><dt className="text-slate-500">Study consistency</dt><dd className="font-bold">{row.studyConsistency}</dd></div><div><dt className="text-slate-500">Open gaps</dt><dd className="font-bold">{row.openGaps}</dd></div><div><dt className="text-slate-500">Completed remedials</dt><dd className="font-bold">{row.completedRemedials}</dd></div></dl></article>)}</section>:<div className="rounded-2xl border bg-white p-8 text-slate-600">No eligible assigned students are available.</div>}</main>;
}
