/**
 * Mock for the 'server-only' module used in test environments.
 * This module is a Next.js convention that prevents server code from
 * being imported in client components. In tests, it's a no-op.
 */
export {};