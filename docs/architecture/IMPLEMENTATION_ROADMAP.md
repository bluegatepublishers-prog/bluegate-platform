# Implementation Roadmap

All phases are **planned**. A phase begins only after its dependencies and business decisions are approved. Every phase requires additive migrations, server-derived ownership, denial-path tests, observability, and rollback notes.

| Phase | Objective and dependencies | Models / routes affected | Security, migration, tests, exit criteria | Risk | Do not build yet |
|---|---|---|---|---|---|
| 8.0 Multi-Tenant Foundation | Add Publisher root and preserve Bluegate; depends on tenant naming/admin mapping decisions. | `Publisher`, publisher membership; top-level `User/School/Book/Resource`; auth/admin APIs. | Nullable keys → seed/backfill → scoped reads/writes → required constraints. Test cross-tenant denial and orphan count zero. Exit: every business record resolves to Bluegate and current journeys pass. | Critical | Domains, Super Admin editing, billing, wholesale UI redesign. |
| 8.1 Branding and Feature Catalog | Resolve authenticated theme and server features; depends 8.0. | `PublisherBranding`, `FeatureDefinition`, `PublisherFeature`, `PlanFeature`; layouts/login/navigation/API guards. | Validate CSS/domain inputs; seed catalog additively. Test disabled routes directly. Exit: two fixture publishers theme/isolate correctly. | High | Public white-label sites, school domains, commercial plan checkout. |
| 8.2 Publisher Staff Permissions | Delegated least-privilege work; depends 8.0–8.1 and staff policy. | `Permission`, `RoleTemplate`, `PublisherStaffMembership`, assignments, `AuditEvent`; admin routes/actions. | Default deny, separation of duties, audited changes. Backfill existing Admin membership. Exit: profiles permit only enumerated operations. | Critical | Support impersonation automation, large organization hierarchy. |
| 8.3 Resource Audience | Prevent student exposure; depends tenant scoping and audience approval. | `Resource.audience`; resource queries/download routes. | Default existing resources conservatively to `TEACHER_ONLY`, review/backfill, then require. Test direct URL/API denial. Exit: audience is explicit everywhere. | High | Student library UI before classification is complete. |
| 8.4 Entitlement and Subscription Foundation | Separate adoption, book entitlement, and feature grants; depends 8.0–8.3 and commercial rules. | Entitlement service; planned `SubscriptionProduct`, `Subscription`, grants; full-PDF routes. | Prefer computed book entitlement; idempotent activation/cancellation; test payer/beneficiary and year boundaries. Exit: one policy service explains allow/deny. | Critical | Payment gateway, persisted entitlements without need, tutor bundles. |
| 9.0 Student Login | Secure student identity and recovery; depends 8.4 and minor policy. | existing `Student.userId`, auth/session, student login/layout. | School-scoped provisioning, no account enumeration, session revocation. Additive user links. Exit: active enrolled student can sign in; inactive/cross-school cannot. | Critical | Learning tools, social login, parent accounts. |
| 9.1 Student Learning Library | Show entitled books and student/both resources; depends 9.0, audience and entitlement. | student dashboard/library, protected downloads. | Explicit selects, signed URLs, school/year checks. Test preview URL leakage and expiry. Exit: fixture matrices match policy. | Critical | AI, assessments, tutor messaging. |
| 9.2 Student Subscription Activation | Grant premium modules from school or individual source; depends 8.4, 9.0–9.1 and payer decisions. | subscriptions/grants, school/student billing views. | Idempotent lifecycle, audit, no card secrets. Exit: activation/cancellation changes premium only, never school/book ownership. | High | Payment collection/refunds/tax automation. |
| 9.3 Assignment Engine | Teacher creates assigned work; depends academic assignments and premium grants. | `Assignment`, recipients, `Submission`, attachments; teacher/student routes. | Teacher scope, immutable due-history/audit, protected files. Exit: assigned students submit/resubmit; outsiders denied. | High | Automated grading and plagiarism. |
| 9.4 Quiz Engine | Interactive practice with safe answer release; depends 9.3/content mapping. | quizzes, questions, attempts/responses. | Server scoring and answer embargo. Exit: retry policy and result consistency tested. | High | Adaptive AI generation. |
| 9.5 Assessment Engine | Formal assessments mapped to outcomes/competencies; depends 9.4. | assessments, attempts, responses, marks, mappings. | Transactional submit, teacher scope, correction audit. Exit: publish/attempt/mark/reassess lifecycle passes. | Critical | High-stakes proctoring. |
| 9.6 Reports | Role-scoped progress aggregation; depends clean assessment data. | report queries/exports. | Tenant/school/student scope, bounded exports, suppression where needed. Exit: aggregates reconcile to source data. | High | Predictive claims and platform benchmarking. |
| 9.7 Gap Analysis | Derive explainable gaps; depends 9.5–9.6 and validated mappings. | gap evidence/recommendations, analytics UI. | Store evidence/version; avoid opaque labels. Exit: each gap traces to scored evidence. | High | Autonomous intervention. |
| 9.8 Remedials | Reviewed interventions and reassessment; depends 9.7/content audience. | `Remedial`, assignment/review/practice links. | Teacher/tutor approval and entitled assets. Exit: recommendation→review→practice→reassessment trace works. | High | Automatic student-facing publication. |
| 9.9 Tutor Platform | Explicitly scoped mentoring; depends student/subscription/remedial and consent policy. | `Tutor`, `TutorAssignment`, notes; tutor routes. | Date/scope/consent, no school-wide discovery, audit. Exit: only assigned students and approved functions visible. | Critical | Open tutor marketplace, payments, video. |
| 10.0 Parent Portal | Linked-child visibility and subscription management; depends verified identity/consent policy. | `Parent/Guardian`, child links; parent routes. | Multi-guardian validation, field-level privacy, link audit. Exit: guardian sees only linked children and approved data. | Critical | Custody adjudication, public messaging network. |

## Delivery gates

Before each release: approved ADRs and data classification; reviewed SQL/backfill metrics; type-check/build/unit/integration/migration tests; direct API denial tests; staged smoke tests for every role; documented rollback. Do not combine critical phases merely to shorten delivery time.
